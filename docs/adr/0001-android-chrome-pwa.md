# ADR 0001: Android Chrome PWA first

Status: Accepted — 2026-08-05

The first client is a mobile-first, installable React/TypeScript PWA targeting Android Chrome. iOS and a native wrapper are outside P0. Model work remains behind `LocalAnalyzer` and outside the UI thread when introduced.

