# Keep independently cached Spring test contexts from exhausting a shared synthetic PostgreSQL
# server during the default full-suite run. Product and personal runtime configuration do not load
# test resources.
spring.datasource.hikari.maximum-pool-size=4
spring.datasource.hikari.minimum-idle=0
