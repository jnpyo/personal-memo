package local.personalmemo.analysis.application;

/** Raw-free reason attached to a model invocation decision. */
public enum AnalysisInvocationReason {
  SEMANTIC_UNCERTAINTY,
  AI_PREFERRED_POLICY
}
