package local.personalmemo.analysis.application;

/** Truth-preserving local knowledge about whether one gateway execution started. */
public enum CloudGatewayExecutionState {
  NOT_STARTED,
  STARTED,
  UNKNOWN
}
